#include "main.h"

/**
 * _puts_recursion - prints a string followed by new line
 * @s: input
 * Return: always 0
 */

void _puts_recursion(char *s)
{
	char s[] = "abcdefghijkl";

	printf("s is :%s\n", s);
	return (0);
}
